Run main.py to start the program. The remaining files contain the processing algorithms; for proper operation, it is recommended not to modify them.

The data.txt file contains test data and serves as an example of the supported data input format.

Make sure to install the necessary dependencies before running the code; they are listed in requirements.txt.
